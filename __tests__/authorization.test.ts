import{describe,it,expect}from"vitest";import{hasPermission,PERMISSIONS,permissionsForRole}from"@/lib/permission-map";
const roles=["SUPER_ADMIN","ADMIN","MANAGER","TEACHER","ACCOUNTANT","PARENT"];
describe("formal role permission matrix",()=>{it("contains only catalogued permissions",()=>{for(const role of roles)for(const permission of permissionsForRole(role))expect(PERMISSIONS).toContain(permission)});it("separates platform administration from school data",()=>{expect(hasPermission("SUPER_ADMIN","platform.manage")).toBe(true);expect(hasPermission("SUPER_ADMIN","children.read")).toBe(false);expect(hasPermission("ADMIN","platform.manage")).toBe(false)});it("limits teachers to educational operations",()=>{for(const p of ["children.read","classes.read","attendance.record","activities.create","homework.create"] as const)expect(hasPermission("TEACHER",p)).toBe(true);for(const p of ["payments.read","staff.update","parents.update","settings.manage"] as const)expect(hasPermission("TEACHER",p)).toBe(false)});it("limits accountants to finance and minimum identity",()=>{for(const p of ["payments.read","payments.create","payments.update","payments.issue_receipt","reports.financial"] as const)expect(hasPermission("ACCOUNTANT",p)).toBe(true);for(const p of ["children.medical.read","complaints.read","homework.read","attendance.read"] as const)expect(hasPermission("ACCOUNTANT",p)).toBe(false)});it("prevents parents from staff mutations",()=>{for(const p of ["children.create","attendance.record","payments.update","staff.read","settings.manage"] as const)expect(hasPermission("PARENT",p)).toBe(false);expect(hasPermission("PARENT","absences.submit")).toBe(true);expect(hasPermission("PARENT","complaints.create")).toBe(true)});it("keeps managers out of finance and platform controls",()=>{expect(hasPermission("MANAGER","reports.operational")).toBe(true);expect(hasPermission("MANAGER","reports.financial")).toBe(false);expect(hasPermission("MANAGER","payments.read")).toBe(false);expect(hasPermission("MANAGER","platform.manage")).toBe(false)});it("separates attendance correction request and approval",()=>{expect(hasPermission("TEACHER","attendance.correct")).toBe(true);expect(hasPermission("TEACHER","attendance.approve")).toBe(false);expect(hasPermission("MANAGER","attendance.approve")).toBe(true);expect(hasPermission("MANAGER","attendance.pickup_manage")).toBe(true)})});
describe("report permission boundaries",()=>{it("keeps operational, financial and platform reports separated",()=>{expect(hasPermission("ADMIN","reports.operational")).toBe(true);expect(hasPermission("ADMIN","reports.financial")).toBe(true);expect(hasPermission("MANAGER","reports.operational")).toBe(true);expect(hasPermission("MANAGER","reports.financial")).toBe(false);expect(hasPermission("ACCOUNTANT","reports.financial")).toBe(true);expect(hasPermission("TEACHER","reports.operational")).toBe(false);expect(hasPermission("PARENT","reports.operational")).toBe(false);expect(hasPermission("SUPER_ADMIN","platform.manage")).toBe(true);expect(hasPermission("SUPER_ADMIN","reports.financial")).toBe(false)})});
