import{describe,it,expect}from"vitest";import{canTransitionComplaint,complaintSla,complaintReference,visibleComplaintMessages}from"@/lib/complaints";
describe("complaint workflow",()=>{it("allows only defined status transitions",()=>{expect(canTransitionComplaint("OPEN","IN_PROGRESS")).toBe(true);expect(canTransitionComplaint("IN_PROGRESS","CLOSED")).toBe(false);expect(canTransitionComplaint("RESOLVED","IN_PROGRESS")).toBe(true);expect(canTransitionComplaint("CLOSED","OPEN")).toBe(false)});it("calculates priority SLA",()=>{const start=new Date("2026-08-16T00:00:00Z");expect(complaintSla("URGENT",start).toISOString()).toBe("2026-08-16T04:00:00.000Z");expect(complaintSla("HIGH",start).toISOString()).toBe("2026-08-17T00:00:00.000Z");expect(complaintSla("NORMAL",start).toISOString()).toBe("2026-08-19T00:00:00.000Z")});it("creates non-identifying references",()=>expect(complaintReference(new Date("2026-01-01"))).toMatch(/^CMP-2026-[A-F0-9]{8}$/));it("never exposes internal notes to parents",()=>{const messages=[{id:"public",internal:false},{id:"secret",internal:true}];expect(visibleComplaintMessages("PARENT",messages).map(x=>x.id)).toEqual(["public"]);expect(visibleComplaintMessages("ADMIN",messages)).toHaveLength(2)})});
