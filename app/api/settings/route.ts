import{NextResponse}from"next/server";import{prisma}from"@/lib/prisma";import{requirePermission}from"@/lib/permissions";import{apiError}from"@/lib/api";import{z}from"zod";import{audit}from"@/lib/audit";
export async function GET(){try{const u=await requirePermission("settings.manage");return NextResponse.json(await prisma.organization.findUnique({where:{id:u.organizationId}}))}catch(e){return apiError(e)}}
export async function PATCH(r:Request){try{const u=await requirePermission("settings.manage");const d=z.object({name:z.string().min(2),phone:z.string().max(30).optional(),email:z.preprocess(v=>v===""?undefined:v,z.string().email().optional()),address:z.string().max(300).optional(),defaultLanguage:z.enum(["fr","ar","en"])}).parse(await r.json());await prisma.organization.update({where:{id:u.organizationId},data:d});await audit(u,"UPDATE","Organization",u.organizationId);return NextResponse.json({ok:true})}catch(e){return apiError(e)}}
