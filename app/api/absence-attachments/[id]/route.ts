import{NextResponse}from"next/server";import{prisma}from"@/lib/prisma";import{getAuthContext}from"@/lib/auth-context";import{ForbiddenError}from"@/lib/auth";import{hasPermission}from"@/lib/permissions";import{privateDownloadUrl,getPrivateObject}from"@/lib/storage";import{apiError}from"@/lib/api";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const ctx=await getAuthContext(),{id}=await params,row=await prisma.absenceAttachment.findFirst({where:{id,organizationId:ctx.organizationId,deletedAt:null},include:{absenceRequest:true}});if(!row)throw new Error("Not found");if(ctx.role==="PARENT"){if(row.absenceRequest.parentId!==ctx.parentId)throw new ForbiddenError("Forbidden")}else if(!hasPermission(ctx.role,"absences.read"))throw new ForbiddenError("Forbidden");const signed=await privateDownloadUrl(row.storageKey,60);if(signed)return NextResponse.redirect(signed);const data=await getPrivateObject(row.storageKey);return new NextResponse(data,{headers:{"content-type":row.mimeType,"content-disposition":`attachment; filename="${row.originalName.replaceAll('"','')}"`,"cache-control":"private, no-store"}})}catch(e){return apiError(e)}}
