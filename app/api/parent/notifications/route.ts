// Backward-compatible parent endpoint. Authorization is handled by the
// account notification handlers and supports every authenticated role.
export { GET, PATCH } from "@/app/api/account/notifications/route";
