import{TokenPasswordForm}from"@/components/account-forms";export default async function Page({params}:{params:Promise<{token:string}>}){const{token}=await params;return <main className="login"><TokenPasswordForm token={token} invitation/></main>}
