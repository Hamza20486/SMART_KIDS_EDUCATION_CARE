import { getTranslations } from "@/lib/i18n/server";
import Link from"next/link";import{prisma}from"@/lib/prisma";import{requirePermission,hasPermission}from"@/lib/permissions";import{CreateForm}from"@/components/create-form";
export const dynamic="force-dynamic";export default async function Page(){const t=await getTranslations();const u=await requirePermission("payments.read"),rows=await prisma.feeCategory.findMany({where:{organizationId:u.organizationId},orderBy:{name:"asc"}});return <><div className="pagehead"><div><h1>{t("staff.feeCategories")}</h1><p className="muted">{t("staff.feeCategoriesHelp")}</p></div><Link href="/admin/payments" className="button secondary">{t("common.back")}</Link></div>{hasPermission(u.role,"payments.create")&&<CreateForm endpoint="/api/fee-categories" title="Nouvelle catégorie" fields={[{name:"name",label:"Nom",required:true},{name:"code",label:"Code (MAJUSCULES)",required:true}]}/>}<table className="table"><thead><tr><th>{t("staff.code")}</th><th>{t("common.name")}</th><th>{t("common.status")}</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.code}</td><td>{x.name}</td><td>{x.active?"Active":"Inactive"}</td></tr>)}</tbody></table></>}
