"use client";import{useRouter}from"next/navigation";import{toast}from"sonner";
import { clientTranslate } from "@/lib/i18n/client";
import { T } from "./i18n-provider";
export function HomeworkSubmit({homeworkId,eligibleChildren}:{homeworkId:string;eligibleChildren:{id:string;name:string}[]}){const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);form.set("homeworkId",homeworkId);const r=await fetch("/api/parent/homework/submissions",{method:"POST",body:form});if(!r.ok){toast.error(clientTranslate("common.retry"));return}toast.success(clientTranslate("common.save"));router.refresh()}return <form className="form" onSubmit={submit}><label><T k="common.child"/><select name="childId">{eligibleChildren.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label><T k="workflow.comment"/><input name="parentComment" maxLength={1000}/></label><label><T k="workflow.attachment"/><input name="file" type="file" accept="application/pdf,image/jpeg,image/png,image/webp"/></label><button className="button"><T k="workflow.submitResubmit"/></button></form>}
