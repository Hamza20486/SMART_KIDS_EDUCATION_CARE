"use client";import{useRouter}from"next/navigation";import{toast}from"sonner";
import { clientTranslate } from "@/lib/i18n/client";
import { T } from "./i18n-provider";
export function AbsenceReview({id}:{id:string}){const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/absences",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:f.get("status"),reviewNote:f.get("reviewNote")})});if(!r.ok){toast.error(clientTranslate("common.retry"));return}toast.success(clientTranslate("common.save"));router.refresh()}return <form className="card form" onSubmit={submit}><h2 style={{gridColumn:"1/-1"}}><T k="workflow.decision"/></h2><label><T k="workflow.decision"/><select name="status"><option value="APPROVED"><T k="workflow.approve"/></option><option value="REJECTED"><T k="workflow.reject"/></option></select></label><label><T k="common.note"/><input name="reviewNote" maxLength={1000}/></label><button className="button"><T k="common.save"/></button></form>}
